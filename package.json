{
  "name": "zkapp-upgrade-tests",
  "version": "0.1.0",
  "description": "zkApp hardfork migration and upgrade test contracts",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.5"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "run": "tsx",
    "setup-feepayers": "tsx setup-feepayers.ts",
    "pre-hf": "tsc && node dist/run-pre-hf.js",
    "post-hf": "tsc && node dist/run-post-hf.js"
  },
  "dependencies": {
    "o1js": "^2.15.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
